apiVersion: v1
kind: Secret
metadata:
  name: argocd-secret
type: Opaque
stringData:
  # Fetch gitlab webhook secret from another resource
  # https://argo-cd.readthedocs.io/en/stable/operator-manual/webhook/#alternative
  webhook.gitlab.secret: $webhook-gitlab:secret
