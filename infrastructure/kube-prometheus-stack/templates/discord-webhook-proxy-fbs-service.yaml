apiVersion: v1
kind: Service
metadata:
  name: discord-webhook-proxy-fbs
  labels:
    app: discord-webhook-proxy
    instance: fbs
spec:
  type: ClusterIP
  ports:
  - name: https
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: discord-webhook-proxy
    instance: fbs
