apiVersion: v1
kind: ConfigMap
metadata:
  name: discord-webhook-proxy-templates
data:
  webhook_proxy.conf.template: |
    server {
      listen 80;

      location / {
        proxy_pass ${DISCORD_WEBHOOK_URL};
      }

      location /health {
        access_log off;
        return 200;
      }
    }
