apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: intranet-wildcard-staging
spec:
  dnsNames:
    - "*{{ .Values.intranet.dnsName }}"
    - "{{ .Values.intranet.dnsName }}"
  issuerRef:
    kind: ClusterIssuer
    name: intranet-dns01-staging
  secretName: intranet-wildcard-staging
