apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: intranet-wildcard
spec:
  dnsNames:
    - "*.{{ .Values.intranet.dnsZone }}"
    - "{{ .Values.intranet.dnsZone }}"
  issuerRef:
    kind: ClusterIssuer
    name: intranet-dns01-prod
  secretName: intranet-wildcard-cert
