apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: traefik-dashboard-external
spec:
  parentRefs:
    - name: traefik-external
  hostnames:
    - "{{ .Values.dashboard.external.hostname }}"
  rules:
    - backendRefs:
        - name: api@internal
          kind: TraefikService
      filters:
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: authelia
