apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: traefik-dashboard-intranet
spec:
  parentRefs:
    - name: traefik-intranet
  hostnames:
    - "{{ .Values.dashboard.intranet.hostname }}"
  rules:
    - backendRefs:
        - name: api@internal
          kind: TraefikService
      filters:
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: authelia
