{{- range $name, $certificate := .Values.external.wildcardCertificates }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ $name }}
spec:
  secretName: {{ default $name $certificate.secretName }}
  issuerRef:
    kind: ClusterIssuer
    name: external-dns01-prod
  dnsNames:
    {{- toYaml $certificate.dnsNames | nindent 4 }}
{{- end }}
