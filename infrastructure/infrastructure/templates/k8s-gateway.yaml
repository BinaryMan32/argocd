apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: k8s-gateway
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.project.name }}
  source:
    repoURL: https://ori-edge.github.io/k8s_gateway/
    chart: k8s-gateway
    targetRevision: 1.0.1
    helm:
      values: |
        image:
          # defaults to "latest" which didn't have an arm64 image
          tag: dd610d7
        domain: {{ .Values.domain.intranet }}
      # 1.0.1 incorrectly adds loadBalancerIP to port, temporarily disable
      # service:
      #   loadBalancerIP: {{ .Values.ipAddress.k8sGateway }}
  destination:
    namespace: k8s-gateway
    server: {{ .Values.destination.server }}
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
