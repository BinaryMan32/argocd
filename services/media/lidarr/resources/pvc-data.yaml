kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: lidarr-data
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "400Gi"
  volumeName: lidarr-data
  storageClassName: ""
