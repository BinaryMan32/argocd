apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: navidrome
  annotations:
    cert-manager.io/cluster-issuer: intranet-ca
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - "from rules"
      secretName: "navidrome-tls"
  rules:
    - host: "navidrome.int.fivebytestudios.com"
      http:
        paths:
          - path: "/"
            pathType: Prefix
            backend:
              service:
                name: navidrome
                port:
                  name: http
