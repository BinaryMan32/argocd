apiVersion: v1
kind: Service
metadata:
  name: audiobookshelf
spec:
  selector:
    app.kubernetes.io/instance: audiobookshelf
    app.kubernetes.io/name: audiobookshelf
  type: ClusterIP
  ports:
    - name: webui
      protocol: TCP
      port: 80
      targetPort: webui
