apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: inventree-db
spec:
  instances: 2
  imageName: ghcr.io/cloudnative-pg/postgresql:15.13

  bootstrap:
    initdb:
      database: inventree
      owner: inventree

  storage:
    size: 1Gi
