kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: sonarr-data
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "400Gi"
  volumeName: sonarr-data
  storageClassName: ""
