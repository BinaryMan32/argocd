apiVersion: v1
kind: PersistentVolume
metadata:
  name: home-assistant-mqtt
spec:
  storageClassName: ""
  claimRef:
    name: mqtt
    namespace: home-assistant
  capacity:
    storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  local:
    path: /var/home-assistant/mqtt
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - pegasus3
