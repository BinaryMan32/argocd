apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mqtt
spec:
  storageClassName: "" # Empty string must be explicitly set otherwise default StorageClass will be set
  volumeName: home-assistant-mqtt
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
