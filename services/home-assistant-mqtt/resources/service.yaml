apiVersion: v1
kind: Service
metadata:
  name: mqtt
spec:
  type: LoadBalancer
  ports:
    - port: 1883
      targetPort: 1883
      protocol: TCP
      name: mqtt
  selector:
    app.kubernetes.io/instance: home-assistant
    app.kubernetes.io/name: mqtt
